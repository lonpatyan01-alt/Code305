/* Placeholder FileSaver - for downloading files */
function saveAs(blob, name){
  // minimal fallback: create link
  var a = document.createElement('a');
  a.href = URL.createObjectURL(blob);
  a.download = name || 'download';
  a.click();
}
